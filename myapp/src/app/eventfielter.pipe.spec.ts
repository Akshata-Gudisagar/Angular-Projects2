import { EventfielterPipe } from './eventfielter.pipe';

describe('EventfielterPipe', () => {
  it('create an instance', () => {
    const pipe = new EventfielterPipe();
    expect(pipe).toBeTruthy();
  });
});
